interface:
  display_name: "Ops Dashboard"
  short_description: "Repo-specific patterns and workflows for ops-dashboard"
  default_prompt: "Use the ops-dashboard repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true